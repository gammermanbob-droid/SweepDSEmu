<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>
    <uses-feature
        android:name="android.software.leanback"
        android:required="false"/>

    <uses-feature android:glEsVersion="0x00030002" android:required="true" />
    <uses-feature android:name="android.hardware.opengles.aep" android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="tiramisu"/>
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- EmulationForegroundService keeps a running 3DS/DS session alive
         while its Activity is merely backgrounded, so briefly switching
         away and back doesn't lose emulation state to Android's
         background process killer; see that class's doc comment. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Lets SweepDSEmuNDSBrewer (a separate, same-signature-only APK,
         see InstallCiaBridgeActivity) hand off a freshly built forwarder
         .cia for installation here, reusing the exact same CiaInstallWorker
         path the in-app "Install CIA" picker already uses. Signature-level
         so only an app signed with this same key can trigger it. -->
    <permission
        android:name="org.citra.citra_emu.permission.INSTALL_CIA_BRIDGE"
        android:protectionLevel="signature" />

    <application
        android:name="org.citra.citra_emu.CitraApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:appCategory="game"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:banner="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">

        <meta-data android:name="android.game_mode_config"
            android:resource="@xml/game_mode_config" />

        <activity
            android:name="org.citra.citra_emu.ui.main.MainActivity"
            android:theme="@style/Theme.Citra.Splash.Main"
            android:exported="true">

            <!-- This intentfilter marks this Activity as the one that gets launched from Home screen. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="org.citra.citra_emu.utils.InstallCiaBridgeActivity"
            android:exported="true"
            android:permission="org.citra.citra_emu.permission.INSTALL_CIA_BRIDGE"
            android:theme="@style/Theme.Citra.Invisible"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />

        <activity
            android:name="org.citra.citra_emu.features.settings.ui.SettingsActivity"
            android:configChanges="orientation|screenSize|uiMode"
            android:theme="@style/Theme.Citra.Main"
            android:label="@string/preferences_settings"/>

        <activity
            android:name="org.citra.citra_emu.activities.DsEmulationActivity"
            android:exported="false"
            android:theme="@style/Theme.Citra.Main"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />

        <service
            android:name="org.citra.citra_emu.services.EmulationForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <activity
            android:name="org.citra.citra_emu.activities.EmulationActivity"
            android:exported="true"
            android:theme="@style/Theme.Citra.Main"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:mimeType="application/octet-stream"
                    android:scheme="content" />
            </intent-filter>

        </activity>

        <activity
            android:name="org.citra.citra_emu.features.cheats.ui.CheatsActivity"
            android:exported="false"
            android:theme="@style/Theme.Citra.Main"
            android:label="@string/cheats"/>
    </application>

</manifest>
